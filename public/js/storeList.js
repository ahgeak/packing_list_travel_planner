const renderStoredList = async (event) => {
    const name = "test"; //add the name of the stored list name here
    const items = "test"; //add the array of items that will be iterated through to be rendered on the page

    console.log(name);
    console.log(items);

};
